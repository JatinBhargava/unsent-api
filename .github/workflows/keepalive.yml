name: Keep Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://unsent-api-1-0-0-snapshot.onrender.com/health > /dev/null